package com.gravitycode.solitaryfitness.util.ui

import android.app.AlertDialog
import android.content.Context
import android.util.Log

class ProgressDialog(context: Context) : AlertDialog(context) {

    override fun show() {
        super.show()
        /*
        * TODO: Start recording time here. If the minimum necessary display time has not been reached by the
        *  time dismiss() is called then wait for the period of the delta.
        * */
    }

    override fun dismiss() {
        super.dismiss()

    }
}